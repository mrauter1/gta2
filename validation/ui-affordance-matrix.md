# Block City UI Affordance Matrix

- Validation run: `bash validation/block-city-run-validation.sh`
- Latest passing report: [validation/artifacts/block-city-run-report.json](/home/rauter/code/gta2/validation/artifacts/block-city-run-report.json)
- Representative passing screenshots:
  - [validation/screenshots/desktop-district-select.png](/home/rauter/code/gta2/validation/screenshots/desktop-district-select.png)
  - [validation/screenshots/desktop-on-foot-reference.png](/home/rauter/code/gta2/validation/screenshots/desktop-on-foot-reference.png)
  - [validation/screenshots/desktop-in-vehicle-hud.png](/home/rauter/code/gta2/validation/screenshots/desktop-in-vehicle-hud.png)
  - [validation/screenshots/mobile-landscape-on-foot.png](/home/rauter/code/gta2/validation/screenshots/mobile-landscape-on-foot.png)
  - [validation/screenshots/mobile-landscape-vehicle.png](/home/rauter/code/gta2/validation/screenshots/mobile-landscape-vehicle.png)
  - [validation/screenshots/mobile-portrait-sanity.png](/home/rauter/code/gta2/validation/screenshots/mobile-portrait-sanity.png)

## Status Legend

- `implemented`: visible and working in the current runtime.
- `implemented with rationale`: visible and intentionally constrained, with the reason stated in-app or in validation.
- `omitted by design`: intentionally hidden to avoid inert UI.

## Menu, District, Pause, And Settings

| Affordance | Source evidence | Implemented location | Selector / UI label | Expected behavior | Validation action | Observed result | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Main menu district flow | parent request + reference gaps | `index.html`, `src/main.js`, `src/ui/hud.js` | `#menuDistrictButton` `Select District` | Open the district catalog from menu state without a reload. | Browser audit clicked the menu button. | District screen became visible and stayed interactive. | implemented |
| Quick deploy saved run | parent request + persistence contract | `index.html`, `src/main.js`, `src/systems/gameplay.js` | `#quickDeployButton` `Deploy Saved Run` | Launch the last run district directly from menu state. | Browser audit returned to menu after changing districts, then clicked quick deploy. | Quick deploy relaunched `Brickline District` from persisted `lastDistrictId`. | implemented |
| Menu settings button | parent request | `index.html`, `src/main.js` | `#menuSettingsButton` `Settings` | Open the live settings drawer from the menu. | Browser audit clicked the button on desktop and touch. | Settings drawer opened in both desktop and touch flows. | implemented |
| District cards | parent request | `src/data/districts.js`, `src/ui/hud.js` | `.district-card` | Show five selectable districts with preview, seed, spawn, rides, missions, and landmarks. | Browser audit counted cards and captured [desktop-district-select.png](/home/rauter/code/gta2/validation/screenshots/desktop-district-select.png). | `5` cards rendered with metadata and preview canvases. | implemented |
| District card persistence | parent request + persistence contract | `src/main.js`, `src/state/game-state.js` | `.district-card[data-district-id='neon-mile']` | Persist the selected district even before launch. | Browser audit selected `Neon Mile`, returned to the menu, then reloaded. | Menu status surface stayed on `Neon Mile` after reload. | implemented |
| District back button | parent request | `index.html`, `src/main.js` | `#districtBackButton` `Back` | Return from district selection to the menu without losing the selected district. | Browser audit used `Back` after selecting `Neon Mile`. | Menu reopened and retained the new district choice. | implemented |
| Launch district | parent request | `index.html`, `src/main.js`, `src/systems/gameplay.js` | `#launchDistrictButton` `Launch ...` | Start the selected district and switch to gameplay state. | Browser audit launched `Sunset Grid` and later `Brickline District`. | Gameplay shell loaded both districts with updated banners and session state. | implemented |
| Pause chip | reference image + parent request | `index.html`, `src/main.js` | `#pauseButton` `Pause` | Open the pause overlay from gameplay. | Browser audit clicked the pause chip. | Pause screen became visible with resume and session options. | implemented |
| Pause resume button | parent request | `index.html`, `src/main.js`, `src/systems/gameplay.js` | `#resumeButton` `Resume` | Resume gameplay without a page reload. | Browser audit resumed from pause on desktop and touch. | HUD returned to `game` state and simulation continued. | implemented |
| Pause settings button | parent request | `index.html`, `src/main.js` | `#pauseSettingsButton` `Settings` | Open the same live settings drawer from pause. | Browser audit opened pause settings. | Settings drawer opened while pause overlay was active. | implemented |
| Reset run button | parent request | `index.html`, `src/main.js`, `src/systems/gameplay.js` | `#resetShellButton` `Reset Run` | Reset the current district session without reloading the app. | Browser audit clicked `Reset Run` from pause and re-read gameplay state. | Session rebuilt into `game` state with the player restored to `(140,560)` at full health and stamina. | implemented |
| Return to districts | parent request | `index.html`, `src/main.js` | `#returnDistrictButton` `Return To Districts` | Leave gameplay and reopen the district selector. | Browser audit used the button after respawn verification. | District selector reopened and a different district was launched afterward. | implemented |
| Volume slider | persistence contract | `index.html`, `src/main.js`, `src/state/game-state.js` | `#volumeSlider` | Save the latest volume value locally. | Browser audit changed the slider to `33`, reloaded, and reread the menu status and slider value. | Reload restored `33% muted` and slider value `33`. | implemented |
| Audio mute toggle | persistence contract | `index.html`, `src/main.js`, `src/state/game-state.js` | `#muteToggle` `Live/Muted` | Toggle mute state and persist it locally. | Browser audit toggled mute on, reloaded, and checked the menu and toggle text. | Reload restored `Muted` with `33% muted` in the menu status card. | implemented |
| Look sensitivity slider | persistence contract | `index.html`, `src/main.js`, `src/systems/input.js` | `#sensitivitySlider` | Save the latest look sensitivity and apply it to drag look. | Browser audit set the slider to `120`, reloaded, and reread the slider value. | Reload restored the slider to `120` and preserved the faster look setting. | implemented |
| Invert look toggle | persistence contract | `index.html`, `src/main.js`, `src/systems/input.js` | `#invertToggle` `Standard/Inverted` | Flip horizontal look direction and persist it locally. | Browser audit toggled invert on, then reloaded and inspected the toggle text. | Reload restored the `Inverted` state. | implemented |
| Touch layout buttons | persistence contract | `index.html`, `src/main.js`, `src/ui/hud.js` | `[data-touch-layout='split']`, `[data-touch-layout='southpaw']` | Swap touch-cluster placement and persist the choice locally. | Browser audit saved `Southpaw`, reloaded, and checked the menu status text. | Reload restored `Southpaw`. | implemented |
| Graphics quality buttons | persistence contract | `index.html`, `src/main.js`, `src/render/three-world.js` | `[data-graphics-quality='auto|high|low']` | Persist graphics preference for the renderer. | Browser audit saved `LOW`, reloaded, and checked the menu status text. | Reload restored `LOW`. | implemented |
| Restore defaults button | persistence contract | `index.html`, `src/main.js`, `src/state/game-state.js` | `#resetSettingsButton` `Restore Defaults` | Reset surfaced settings to sensible defaults. | Browser audit clicked `Restore Defaults` after persisting custom settings and reread surfaced values. | Settings returned to `65% live`, `Split`, and `AUTO`. | implemented |
| Session mode note | parent multiplayer requirement | `index.html`, `src/ui/hud.js` | menu `Mode`, settings `Session mode` | Explain that local solo play is supported and multiplayer is intentionally unavailable. | Browser audit inspected the menu and settings copy. | Both surfaces state `Local solo only` and explain why no matchmaking control is shown. | implemented with rationale |
| Multiplayer selector / matchmaking button | parent multiplayer requirement | intentionally not rendered | none | Stay hidden unless multiplayer actually exists. | Browser audit inspected menu, district, pause, settings, and HUD. | No inert multiplayer control is shown anywhere. | omitted by design |

## HUD, Prompts, Slots, And Status Surfaces

| Affordance | Source evidence | Implemented location | Selector / UI label | Expected behavior | Validation action | Observed result | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Minimap panel | reference image + parent request | `index.html`, `src/ui/hud.js` | `#minimapCanvas` | Render a readable top-left district map in desktop and mobile states. | Browser audit captured all gameplay screenshots. | Minimap stayed visible across desktop, mobile landscape, and portrait states. | implemented |
| North badge | reference image | `index.html` | `.north-badge` `N` | Mark minimap orientation. | Screenshot review. | `N` badge remains anchored over the minimap in every gameplay screenshot. | implemented |
| Player arrow | reference image + parent request | `src/ui/hud.js` | cyan player triangle on `#minimapCanvas` | Track current player position and facing. | Browser audit moved on foot, drove, and reviewed the minimap render. | Player arrow moved during both keyboard and touch traversal checks. | implemented |
| Route line | reference image + mission contract | `src/ui/hud.js` | yellow route line on `#minimapCanvas` | Connect current position to the active objective. | Browser audit accepted and completed the package mission. | Route line remained visible and updated through the mission legs. | implemented |
| Vehicle icon | reference image | `src/ui/hud.js` | white vehicle block on `#minimapCanvas` | Mark the hero vehicle. | Browser audit reviewed on-foot and in-vehicle screenshots. | Vehicle icon remained visible on the minimap while near or inside the ride. | implemented |
| Home or garage icon | reference image | `src/ui/hud.js` | green house marker on `#minimapCanvas` | Mark the cooldown or garage return point. | Browser audit triggered slot `5` and heat decay near the home lane. | Home icon stayed visible and the cooldown lane reduced heat as expected. | implemented |
| Package or mission icon | reference image + mission contract | `src/ui/hud.js` | purple square objective marker on `#minimapCanvas` | Mark live mission objectives. | Browser audit accepted the package run and advanced both stages. | Mission icon stayed live for pickup and dropoff legs. | implemented |
| Mission card shell | reference image + mission contract | `index.html`, `src/ui/hud.js` | `.mission-panel` | Show title, stage, helper copy, reward, timer, and distance. | Browser audit captured gameplay screenshots and completed a mission. | Panel stayed live in free roam, active mission, and completion states. | implemented |
| Mission icon and title | reference image | `src/ui/hud.js` | `#missionIcon`, `#missionTitle` | Reflect the nearest or active mission type. | Browser audit observed `PARCEL RUN` and `PKG` in screenshots. | Card updated from the run board to the active package mission. | implemented |
| Mission distance readout | reference image + mission contract | `src/ui/hud.js` | `#missionDistance` | Update distance to the current target. | Browser audit advanced the package run and captured screenshots before and after entry. | Distance values changed from `204m` to `200m` and updated during mission routing. | implemented |
| Cash counter | reference image + reward loop | `index.html`, `src/ui/hud.js` | `#cashValue` | Increase after mission success. | Browser audit completed `PARCEL RUN`. | Cash rose from `$2,450` to `$2,800`. | implemented |
| Weapon status panel | parent request + combat contract | `index.html`, `src/ui/hud.js` | `#weaponLabel`, `#weaponMode`, `#weaponAmmo`, `#weaponStatus` | Surface the live sidearm name, draw state, ammo, and honest fire or reload guidance. | Browser audit drew the sidearm, fired it, reloaded it, and captured combat screenshots. | The HUD surfaced `CINDER-9`, switched to `DRAWN`, decremented clip ammo, and exposed reload state truthfully. | implemented |
| Combat reticle | parent request + combat contract | `index.html`, `src/ui/hud.js` | `#reticle` | Become visible only while the on-foot sidearm is drawn. | Browser audit drew slot `1`, fired, and reviewed the follow-up HUD state. | Reticle visibility toggled on with the drawn sidearm and stayed hidden while holstered or driving. | implemented |
| Heat star row | reference image + heat contract | `index.html`, `src/ui/hud.js` | `#starRow` | Show current heat level and react to pressure. | Browser audit raised heat with handbrake and forced a high-heat search state. | Heat increased in desktop and touch vehicle checks and activated search zones above `3`. | implemented |
| Health bar | reference image + failure loop | `index.html`, `src/ui/hud.js` | `#healthFill`, `#healthValue` | Track player health and reset on respawn. | Browser audit forced a player failure and then completed the respawn loop. | Health restored to `100` after respawn. | implemented |
| Stamina bar | reference image | `index.html`, `src/ui/hud.js` | `#staminaFill`, `#staminaValue` | Drain while sprinting and recover over time. | Browser audit sprinted forward with `Shift + W`. | Stamina dropped from `100` to `99.20800000000003` during the keyboard movement check. | implemented |
| Vehicle durability bar | parent request | `index.html`, `src/ui/hud.js` | `#durabilityFill`, `#durabilityValue` | Track ride damage and feed failure or respawn. | Browser audit drove, reset the ride, and verified respawn. | Durability changed during driving and restored to `92` after respawn. | implemented |
| Speedometer | reference image + vehicle contract | `index.html`, `src/ui/hud.js` | `#speedValue`, `#speedNeedle` | Show speed and mode in vehicle play, remain present but calm on foot. | Browser audit captured desktop and mobile vehicle screenshots. | Speedometer displayed live speed values in vehicle mode and `0 KM/H` at rest. | implemented |
| Build tag | reference image | `index.html` | `.build-tag` | Mark the runtime as a browser build. | Screenshot review. | Lower-left build tag stays visible on desktop and mobile screenshots. | implemented |
| Enter or exit prompt | reference image + vehicle loop | `index.html`, `src/ui/hud.js` | `#enterPrompt` `E Enter Ride / Exit Ride` | Show contextual vehicle entry or exit guidance. | Browser audit staged on-foot and in-vehicle states, then used `E` and touch ride/exit buttons. | Prompt changed from `Enter Ride` to `Exit Ride` as the player entered the sedan. | implemented |
| Interact or accept prompt | reference image + mission contract | `index.html`, `src/ui/hud.js` | `#interactPrompt` `F Accept Run / Interact` | Show contextual mission accept or objective interaction guidance. | Browser audit accepted the package run and advanced mission stages with `F`. | Prompt changed between `Accept Run`, disabled guidance, and live interaction copy. | implemented |
| Slot 1 sidearm draw | parent request + reference adaptation | `index.html`, `src/systems/gameplay.js` | `#slotOne` `Cinder-9` | Draw or holster the original sidearm and light up the combat HUD honestly. | Browser audit clicked slot `1`, then fired from keyboard and mouse. | Slot `1` equipped the sidearm, exposed the reticle, and enabled the live combat loop. | implemented |
| Slot 2 route pulse | reference image adaptation | `index.html`, `src/systems/gameplay.js` | `#slotTwo` `Route Kit` | Pulse the active mission or dispatch route line. | Browser audit clicked the slot during desktop gameplay. | Route pulse timer jumped and the slot became the active utility action. | implemented |
| Slot 3 horn or whistle | reference image adaptation | `index.html`, `src/systems/gameplay.js` | `#slotThree` `Horn` | Trigger a horn in the ride or a whistle on foot. | Browser audit clicked slot `3` during desktop gameplay. | `hornPulseTimer` rose above `0.1` and the active slot changed to `3`. | implemented |
| Slot 4 ride reset | parent request | `index.html`, `src/systems/gameplay.js` | `#slotFour` `Reset` | Recover the ride at the nearest safe spawn. | Browser audit clicked slot `4` after a boundary impact. | Vehicle reset back to a valid nearby spawn with control preserved. | implemented |
| Slot 5 garage ping | parent request + garage icon | `index.html`, `src/systems/gameplay.js` | `#slotFive` `Garage` | Pulse the home or garage point. | Browser audit clicked slot `5` and inspected `homePingTimer`. | `homePingTimer` jumped above `4.9` seconds. | implemented |
| Field bag button | reference image adaptation | `index.html`, `src/main.js` | `#inventoryButton` `BAG` | Open the field bag drawer. | Browser audit clicked the button and closed the drawer again. | Inventory drawer opened and exposed live route and street-tool notes. | implemented |

## Desktop, Touch, And Mobile-Specific Controls

| Affordance | Source evidence | Implemented location | Selector / UI label | Expected behavior | Validation action | Observed result | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Desktop movement keys | parent request | `src/systems/input.js`, `src/systems/gameplay.js` | `WASD / arrows` | Move on foot and drive in vehicle mode. | Browser audit held `Shift + W` and measured player movement. | Player advanced from `(140,560)` to `(154.9582800653268,566.113731777361)`. | implemented |
| Desktop mouse look | parent request | `src/systems/input.js` | drag on `#sceneCanvas` | Rotate camera yaw. | Browser audit dragged the scene canvas. | Camera yaw moved from `0.34` to `0.6255999999999999`. | implemented |
| Desktop `E` ride key | parent request + reference image | `src/systems/input.js`, `src/systems/gameplay.js` | `KeyE` | Enter or exit the nearby ride. | Browser audit pressed `E` from on-foot state. | Session mode changed to `vehicle`. | implemented |
| Desktop `F` interact key | parent request + reference image | `src/systems/input.js`, `src/systems/gameplay.js` | `KeyF` | Accept a mission or clear an objective. | Browser audit pressed `F` at the garage and again at mission markers. | `PARCEL RUN` started and later completed with a cash payout. | implemented |
| Desktop fire controls | parent request + combat contract | `src/systems/input.js`, `src/main.js`, `src/systems/gameplay.js` | `KeyC`, scene click | Fire the drawn on-foot sidearm from keyboard or mouse input. | Browser audit fired once with `C` and once with a scene click against patrol contact. | Both inputs consumed ammo, produced hits or search pressure, and raised heat. | implemented |
| Desktop reload control | parent request + combat contract | `src/systems/input.js`, `src/main.js`, `src/systems/gameplay.js` | `KeyR` | Start and complete sidearm reloads from the surfaced control. | Browser audit emptied the clip, pressed `R`, then advanced simulation until reload completion. | Reload timer started, then the clip refilled and the pending flag cleared. | implemented |
| Desktop `B` field bag key | reference adaptation | `src/systems/input.js`, `src/main.js` | `KeyB` | Toggle the inventory drawer. | Source inspection and live button coverage. | Keyboard binding is wired to the same field-bag drawer as the `BAG` button. | implemented |
| Desktop keyboard hint strip | parent request | `index.html` | `.desktop-hints` | Show desktop-only control hints. | Screenshot review. | Hint strip is visible on desktop screenshots and compacted out of touch-first evidence. | implemented |
| Touch movement pad | parent request | `index.html`, `src/systems/input.js` | `[data-touch-move]` cluster | Move on foot or steer in vehicle mode. | Browser audit held the `up` pad and later the `left` pad in vehicle mode. | On-foot movement advanced the player; steering changed vehicle heading. | implemented |
| Touch look pad | parent request | `index.html`, `src/systems/input.js` | `#lookPad` `LOOK DRAG` | Rotate the camera without a mouse. | Browser audit dragged the look pad. | Camera yaw moved from `0.34` to `0.6154`. | implemented |
| Touch fire button | parent request + combat contract | `index.html`, `src/main.js`, `src/systems/gameplay.js` | `#touchFireButton` `Fire` | Draw and fire the on-foot sidearm without a keyboard or mouse. | Browser audit tapped `Fire` in mobile landscape. | Sidearm shots fired, ammo fell by `1`, and heat rose on touch. | implemented |
| Touch ride button | parent request | `index.html`, `src/main.js` | `#touchRideButton` `Ride` | Enter the nearby ride on touch devices. | Browser audit staged the player next to the sedan and tapped `Ride`. | Session mode switched to `vehicle`. | implemented |
| Touch use button on foot | parent request | `index.html`, `src/main.js` | `#touchInteractButton` `Use` | Accept a run or interact on foot. | Browser audit tapped `Use` while on foot and captured the live toast response. | The interaction toast changed from `Sunset Grid loaded` to `No live contract nearby`. | implemented |
| Touch pause button on foot | parent request | `index.html`, `src/main.js` | `#touchPauseButton` `Pause` | Pause while on foot. | Browser audit tapped the on-foot `Pause` button on mobile. | Pause overlay opened successfully on mobile before gameplay resumed. | implemented |
| Touch exit button | parent request | `index.html`, `src/main.js` | `#touchExitButton` `Exit` | Leave the ride and return to on-foot control. | Browser audit tapped `Exit` after vehicle checks. | Session mode returned to `foot`. | implemented |
| Touch use button in vehicle | parent request | `index.html`, `src/main.js` | `#touchVehicleInteractButton` `Use` | Interact from the driver state. | Browser audit tapped `Use` after entering the sedan and captured the live toast response. | The interaction toast changed from `Entered parked sedan` to `No live contract nearby`. | implemented |
| Touch handbrake | parent request | `index.html`, `src/systems/input.js`, `src/systems/gameplay.js` | `#touchHandbrakeButton` `Handbrake` | Apply drift-lite braking and raise pressure. | Browser audit held the control in vehicle mode. | Heat rose from `0.8574400000000013` to `0.8693200000000012`. | implemented |
| Touch brake or reverse | parent request | `index.html`, `src/systems/input.js`, `src/systems/gameplay.js` | `#touchBrakeButton` `Brake / Rev` | Slow or reverse the vehicle. | Browser audit held the control after accelerating. | Vehicle speed changed from `25.32025142907066` to `-5.9629704294202215`. | implemented |
| Touch throttle | parent request | `index.html`, `src/systems/input.js`, `src/systems/gameplay.js` | `#touchThrottleButton` `Accelerate` | Accelerate the vehicle. | Browser audit held the control in vehicle mode. | Vehicle speed increased from `0` to `28.745891328`. | implemented |
| Touch pause button in vehicle | parent request | `index.html`, `src/main.js` | `#touchVehiclePauseButton` `Pause` | Pause while driving. | Browser audit tapped the button in vehicle mode. | Pause overlay opened successfully on mobile. | implemented |

## World, Mission, And System Affordances

| Affordance | Source evidence | Implemented location | Selector / UI label | Expected behavior | Validation action | Observed result | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Third-person on-foot avatar | parent request + reference image | `src/render/three-world.js` | world actor | Keep a visible avatar in frame during traversal. | Screenshot review plus movement checks. | Avatar is visible in desktop and mobile on-foot evidence. | implemented |
| Nearby first vehicle | reference image + feel contract | `src/data/districts.js`, `src/render/three-world.js` | parked sedan in `Sunset Grid` | Present a clear early ride target. | Browser audit entered the parked sedan on desktop and touch. | The hero sedan stayed within reach of the spawn route and mission staging. | implemented |
| Vehicle driving loop | parent request | `src/systems/gameplay.js` | vehicle mode | Support throttle, steering, brake, reverse, and handbrake. | Browser audit drove with keyboard and touch controls. | All required drive inputs changed speed or heading as expected. | implemented |
| Ride reset if stuck | parent request | `src/systems/gameplay.js` | slot `4` `Reset` | Recover the ride to a valid spawn. | Browser audit triggered slot `4` after the east-boundary collision check. | Vehicle snapped back to a safe spawn with restored placement. | implemented |
| Package mission | parent request + reference image | `src/state/game-state.js`, `src/systems/gameplay.js` | `PARCEL RUN` | Accept, tag pickup on foot, then deliver by vehicle. | Browser audit accepted and completed the run. | Run started at the garage, advanced to stage `1`, and paid out on dropoff. | implemented |
| Checkpoint mission | parent request | `src/state/game-state.js` | `CHECKPOINT DASH` | Clear timed vehicle checkpoints. | Source inspection of generated mission scripts. | Timed gate script is present with vehicle-only stages and a `42s` timer. | implemented |
| Crew-lift mission | parent request | `src/state/game-state.js` | `CREW LIFT` | Reach pickup, then drive the rider home. | Source inspection of generated mission scripts. | Two-stage pickup and return mission is present and repeatable. | implemented |
| Mission reward payout | parent request | `src/systems/gameplay.js` | cash increase | Reward mission completion. | Browser audit completed `PARCEL RUN`. | Cash increased by `$350` from `$2,450` to `$2,800`. | implemented |
| Heat and search pressure | parent request | `src/systems/gameplay.js`, `src/ui/hud.js` | heat stars + search zones | Raise heat from reckless play and activate search pressure at higher levels. | Browser audit used handbrake, then forced a high-heat search state. | Heat rose during drive tests and search zones became active above level `3`. | implemented |
| Heat decay | parent request | `src/systems/gameplay.js` | home lane cooldown | Let heat fall when the player reaches the safe or cooldown zone. | Browser audit staged the player at the garage lane and waited. | Heat dropped from `2.4` to `2.3076000000000008`. | implemented |
| Traffic vehicles | parent request + reference image | `src/data/world-layout.js`, `src/render/three-world.js` | moving world actors | Populate the streets with ambient vehicles. | Screenshot review plus source inspection. | Multiple moving traffic actors appear in desktop and mobile vehicle screenshots. | implemented |
| Pedestrians | parent request + reference image | `src/data/world-layout.js`, `src/render/three-world.js` | moving world actors | Populate sidewalks or verges with walkers or idlers. | Screenshot review plus source inspection. | Pedestrian actors are visible in gameplay screenshots and animate through the layout system. | implemented |
| Garage or home return cue | reference image + parent request | `src/systems/gameplay.js`, `src/ui/hud.js` | slot `5`, minimap house icon | Provide a clear safe or garage anchor. | Browser audit triggered the garage ping and later used the home lane for heat decay. | Home ping activated and the cooldown lane behaved as expected. | implemented |
| Failure and respawn loop | parent request | `src/systems/gameplay.js` | toast + mode banner | Recover from player or vehicle failure without a reload. | Browser audit forced player failure and advanced simulation through respawn. | Session returned to `foot` with `100` health and `92` durability. | implemented |
| Local solo notice | parent request | menu status + settings copy | `Local solo only` | Explain local play instead of exposing inert multiplayer UI. | Browser audit inspected both menu and settings copy. | Messaging stays consistent across menu and settings views. | implemented with rationale |
| Direct firearm combat loop | parent request + reference adaptation | `index.html`, `src/main.js`, `src/systems/gameplay.js`, `src/ui/hud.js` | slot `1`, `#reticle`, `KeyC`, scene click, `#touchFireButton` | Provide a fully working original sidearm with honest HUD feedback, hits or blocks, reload, and heat integration. | Browser audit drew the sidearm, fired on desktop and touch, reloaded, validated blocked shots, and captured combat screenshots. | `CINDER-9` shots produced hits or blocker stops, raised heat, triggered actor reactions, and worked across desktop and touch input. | implemented |
