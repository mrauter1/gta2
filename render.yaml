services:
  - type: web
    name: gta2
    runtime: static
    repo: https://github.com/mrauter1/gta2
    branch: main
    buildCommand: "mkdir -p public && printf '%s' \"$SITE_TGZ_B64\" | base64 -d | tar -xzf - -C public"
    staticPublishPath: public
    autoDeployTrigger: commit
